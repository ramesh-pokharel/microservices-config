server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: product
          uri: lb://product-service
          predicates:
            - Path=/products/**
        - id: order
          uri: lb://order-service
          predicates:
            - Path=/orders/**